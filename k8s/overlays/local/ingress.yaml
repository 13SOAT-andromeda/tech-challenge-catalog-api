apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: catalog-api-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /v1
            pathType: Prefix
            backend:
              service:
                name: catalog-api-svc
                port:
                  number: 80
