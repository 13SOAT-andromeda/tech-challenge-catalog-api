apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - ../../base
  - config.yaml
  - ingress.yaml
  - postgres.yaml

patches:
  - target:
      kind: Deployment
      name: catalog-api
    patch: |-
      - op: replace
        path: /spec/template/spec/containers/0/imagePullPolicy
        value: Never
